import { HttpStatus } from '@nestjs/common';

export const statusBadRequest = HttpStatus.BAD_REQUEST;
export const statusNotFound = HttpStatus.UNAUTHORIZED;
export const statusUnauthorized = HttpStatus.UNAUTHORIZED;
export const statusOk = HttpStatus.OK;
export const statusCreated = HttpStatus.CREATED;
